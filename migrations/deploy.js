const path = require("path");
const programDir = path.join(__dirname, "..", "target", "deploy");

module.exports = async function (provider) {
  // Migration deploy script placeholder
  console.log("Holdify migration deploy placeholder");
};
